import React from 'react';
import {
  Box,
  Typography
} from '@mui/material';

const Main = (props) => {
  return (
    <Box>
      <Typography align='center' variant='h1'>Welcome to My Website!</Typography>
      <Typography align='center'>Sorry about the dust, this is still a work in progress. I wanted to get something up though, so I put this together pretty quick.</Typography>
    </Box>
  );
};

export default Main;
